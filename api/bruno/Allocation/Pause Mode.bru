meta {
  name: Pause Mode
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/user/{{userId}}/allocation/pause
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
