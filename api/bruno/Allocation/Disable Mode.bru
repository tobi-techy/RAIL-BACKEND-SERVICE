meta {
  name: Disable Mode
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/allocation/disable
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Disable 70/30 Allocation Mode
  
  Disable automatic allocation split. Deposits will no longer be automatically split.
}
