meta {
  name: Enable Mode
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/allocation/enable
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Enable 70/30 Allocation Mode
  
  Enable automatic 70/30 split for all incoming deposits:
  - 70% → Spending (liquid, card access)
  - 30% → Stash (auto-invested via Alpaca)
}
