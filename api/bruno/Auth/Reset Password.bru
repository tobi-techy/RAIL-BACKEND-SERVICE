meta {
  name: Reset Password
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "reset-token",
    "password": "NewSecurePass123!"
  }
}
