meta {
  name: Drift
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/rebalancing/configs/{{configId}}/drift
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
