meta {
  name: Create Config
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/rebalancing/configs
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "My Config",
    "allocations": {
      "AAPL": "50.0",
      "MSFT": "50.0"
    },
    "threshold_pct": "5.0",
    "frequency": "monthly"
  }
}
