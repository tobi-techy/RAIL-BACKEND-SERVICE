meta {
  name: Get Config
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/rebalancing/configs/{{configId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
