meta {
  name: Fund Account
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/investment/fund
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "amount": "100.00"
  }
}
