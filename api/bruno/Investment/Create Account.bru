meta {
  name: Create Account
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/investment/account
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "type": "trading_cash"
  }
}
