meta {
  name: Update
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/{{apiVersion}}/scheduled-investments/{{scheduledInvestmentId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "amount": "50.00",
    "frequency": "monthly"
  }
}
