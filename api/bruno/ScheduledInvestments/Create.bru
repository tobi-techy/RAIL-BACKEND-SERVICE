meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/scheduled-investments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "amount": "25.00",
    "frequency": "weekly"
  }
}
