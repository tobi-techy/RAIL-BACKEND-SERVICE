meta {
  name: Resume
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/scheduled-investments/{{scheduledInvestmentId}}/resume
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
