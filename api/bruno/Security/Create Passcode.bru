meta {
  name: Create Passcode
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/security/passcode
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "passcode": "1234",
    "confirmPasscode": "1234"
  }
}
