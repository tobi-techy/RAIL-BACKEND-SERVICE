meta {
  name: Delete Passkey
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/api/{{apiVersion}}/security/passkeys/{{passkeyId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
