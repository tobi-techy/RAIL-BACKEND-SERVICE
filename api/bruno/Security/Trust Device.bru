meta {
  name: Trust Device
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/security/devices/{{deviceId}}/trust
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
