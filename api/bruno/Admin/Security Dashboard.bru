meta {
  name: Security Dashboard
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/admin/security/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
