meta {
  name: Resume Draft
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/copy/drafts/{{draftId}}/resume
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
