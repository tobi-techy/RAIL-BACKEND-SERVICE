meta {
  name: Change Password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/users/me/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "currentPassword": "SecurePass123!",
    "newPassword": "NewSecurePass123!"
  }
}
