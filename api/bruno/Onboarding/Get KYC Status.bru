meta {
  name: Get KYC Status
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/kyc/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Get KYC Status
  
  Per PRD: KYC status gates funding features.
  
  Returns:
  - status: pending, approved, rejected
  - tier: basic, enhanced
  - limits based on tier
}
