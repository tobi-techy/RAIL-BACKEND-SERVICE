meta {
  name: Create Card
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/cards
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "type": "virtual"
  }
}
