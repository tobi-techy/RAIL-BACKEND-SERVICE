meta {
  name: Create Virtual Card
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/cards
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "type": "virtual"
  }
}

docs {
  # Create Virtual Card
  
  Per PRD Epic 4: Virtual card usable immediately after funding.
  
  Card is linked directly to Spend Balance for real-time authorization.
}
