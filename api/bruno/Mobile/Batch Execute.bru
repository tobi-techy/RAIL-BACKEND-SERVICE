meta {
  name: Batch Execute
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/mobile/batch
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "requests": []
  }
}
