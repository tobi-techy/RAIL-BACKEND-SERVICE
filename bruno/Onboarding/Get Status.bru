meta {
  name: Get Onboarding Status
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/onboarding/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Get Onboarding Status
  
  Returns current onboarding progress including:
  - KYC status (gates funding features)
  - Wallet provisioning status
  - Account setup completion
}
