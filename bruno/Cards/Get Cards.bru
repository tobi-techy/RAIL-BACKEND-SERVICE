meta {
  name: Get Cards
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/cards
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Get User Cards
  
  Per PRD Epic 4: Virtual debit card linked to Spend Balance.
  
  Returns all cards for the user (virtual and physical).
}
