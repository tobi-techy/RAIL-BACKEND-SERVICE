meta {
  name: Get Wallet Addresses
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/wallet/addresses
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

query {
  chain: eth
}

docs {
  # Get Wallet Addresses
  
  Returns deposit addresses for supported chains:
  - eth (Ethereum)
  - polygon
  - bsc
  - sol (Solana)
}
