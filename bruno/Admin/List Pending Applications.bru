meta {
  name: List Pending Applications
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/admin/copy/applications
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

query {
  page: 1
  page_size: 20
}

docs {
  # List Pending Conductor Applications (Admin)
  
  Returns all pending conductor applications awaiting review.
}
