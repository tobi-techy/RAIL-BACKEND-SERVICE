meta {
  name: Create Virtual Account
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/funding/virtual-account
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "currency": "USD"
  }
}

docs {
  # Create Virtual Account (USD/GBP)
  
  Per PRD Epic 2: Virtual Accounts for bank transfers
  - Supports USD and GBP currencies
  - Dedicated virtual account for fiat deposits
  - Powered by Bridge integration
}
