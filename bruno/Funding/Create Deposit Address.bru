meta {
  name: Create Deposit Address (Crypto)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/funding/deposit/address
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "chain": "ethereum"
  }
}

docs {
  # Create Crypto Deposit Address
  
  Per PRD Epic 2: Multi-Chain USDC Deposits
  
  Supported chains:
  - ethereum
  - polygon
  - bsc (Binance Smart Chain)
  - solana
  
  USDC deposits trigger automatic 70/30 split.
}
