meta {
  name: Get Transaction History
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/funding/transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

query {
  limit: 20
  offset: 0
}
