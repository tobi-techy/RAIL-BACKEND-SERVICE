replicaCount: 5

image:
  repository: ghcr.io/stack/stack-service
  pullPolicy: IfNotPresent
  tag: "latest"

ingress:
  hosts:
    - host: api.stack-platform.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: stack-service-tls
      hosts:
        - api.stack-platform.com

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 5
  maxReplicas: 20
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

environment: production
