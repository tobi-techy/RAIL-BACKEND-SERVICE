apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rail-service
  labels:
    app: rail-service
spec:
  selector:
    matchLabels:
      app: rail-service
  endpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
