apiVersion: v1
kind: ConfigMap
metadata:
  name: rail-service-config
  labels:
    app: rail-service
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "info"
  SERVER_PORT: "8080"
  SERVER_READ_TIMEOUT: "30"
  SERVER_WRITE_TIMEOUT: "30"
  RATE_LIMIT_PER_MIN: "100"
